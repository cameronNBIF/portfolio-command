{
  "name": "@portfolio-command/metrics",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "capture:fixtures": "tsx test/harness/capture.ts",
    "verify:fixtures": "tsx test/harness/capture.ts --check"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "vitest": "^3.1.0"
  },
  "dependencies": {
    "@portfolio-command/contract": "*"
  }
}
