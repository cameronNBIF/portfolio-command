/**
 * Pure metric functions and golden-master fixtures (phase A1).
 * No React, no database, no I/O. Definitions are frozen at the prototype's
 * implementations (ADR-013): moic, fundMetrics, xirr, fiMetrics, healthAlerts,
 * suggestedReserve, runScenario, leverage, FMV growth, same-store revenue.
 */
export {};
