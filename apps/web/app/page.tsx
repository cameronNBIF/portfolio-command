// Placeholder. The eight-tab UI ports one-to-one from the prototype at
// phase A2, against docs/reference/demo.json as a static fixture (ADR-014).
export default function Home() {
  return <main>Portfolio Command - scaffold (A0.1). UI arrives at A2.</main>;
}
